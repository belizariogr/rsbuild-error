import React from 'react';
import { getFieldLabel, getFieldOptionText } from '../../services/Lang';
import { useLang } from '../../services/Lang';
import Utils, { formatValue } from '../../services/Utils';
import { getPropValue } from '../../services/Utils/workarounds';
import { Field, IReport } from '../../types';
import useStyles from './styles';

const ListReport: React.FC<IReport> = ({ dataset, data, classes }) => {



    return (
        <div>

        </div>
    )
};

export default ListReport;